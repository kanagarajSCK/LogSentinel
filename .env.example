# Optional LLM API key for AI-powered incident analysis.
# When absent, the system uses rule-based analysis as a fallback.
# The application works fully without this — AI is an enhancement layer.
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_API_URL=https://api.openai.com/v1/chat/completions
